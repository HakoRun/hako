# Rust build output
/target
/target-s

# Local hako workspace state (dogfooding `hako init` anywhere in the repo
# tree). Workspace state — objects, refs, session — is never committed.
.hako/

# Backup files from rustfmt
**/*.rs.bk

# Editor / OS cruft
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db

# Local environment overrides
.env
*.local
