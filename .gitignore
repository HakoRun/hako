# Rust build output
/target
/target-s

# Local hako workspace state (dogfooding `hako init` in the repo root).
# Workspace state — objects, refs, session — is never committed.
.hako/

# Backup files from rustfmt
**/*.rs.bk

# Editor / OS cruft
.idea/
.vscode/
*.swp
*~
.DS_Store
Thumbs.db

# Local environment overrides
.env
*.local
